import React from "react";
import { View, StyleSheet, Text } from "react-native";

const ReelViewScreen = () => {
  return (
    <>
      <Text style={{ fontSize: 48 }}>ReelViewScreen</Text>
    </>
  );
};

const styles = StyleSheet.create({});

export default ReelViewScreen;
